const numberOfElements = document.querySelectorAll(`#categories .item`);
console.log(numberOfElements.length);

const getHeader = document.querySelectorAll(`h2`)
getHeader.classList.toggle(`new-class`);
console.log(getHeader.innerHTML);
